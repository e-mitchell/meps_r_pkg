% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_total_sops.R
\name{add_total_sops}
\alias{add_total_sops}
\title{Sum doctor + facility payments for hospital event}
\usage{
add_total_sops(df)
}
\arguments{
\item{df}{MEPS event data (hospital only)}
}
\value{
data frame with summarized expenditures. Can contain original variable names (with event-type prefix) or standardized names with event prefix removed.
}
\description{
Creates total expenditure variable for each source of payment (e.g. Out of pocket, Medicaid, Medicare) by adding doctor and facility expenditures. Applies only to hospital event data: Inpatient, Outpatient, and Emergency Room.
}
\examples{

ER <- read_MEPS(type='ER',year=2014,web=T)
head(ER)

# Add total SOPs
ER2 <- add_total_sops(ER)
head(ER2)

# Add total SOPs after removing 'ER' prefix
ER_bare <- ER \%>\% rm_evnt_key('ER')
ER3 <- add_total_sops(ER_bare)
head(ER3)
}
